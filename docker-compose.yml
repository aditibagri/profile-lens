services:
  api:
    build: .
    image: linkedin-profile-api
    container_name: linkedin-profile-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      PORT: "8000"
    restart: unless-stopped
